$(document).ready(function() {
  $("#imagelist").tablesorter({
        sortList: #{sortColumns},
        theme: "bootstrap",
        headerTemplate: "{content} {icon}",
        widgets : [ "uitheme", "filter" ],
        widgetOptions : {
            filter_hideFilters: false,
        }
    });
});
