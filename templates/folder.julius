$(document).ready(function() {
  $("#folder").tablesorter({
        sortList: [[0,0]],
        theme: "bootstrap-simple",
        headerTemplate: "{content} {icon}",
        widgets : [ "uitheme" ],
    });
});
