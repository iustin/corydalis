$(document).ready(function() {
  $("#folder").tablesorter({
        sortList: [[1,0]],
        theme: "bootstrap",
        headerTemplate: "{content} {icon}",
        widgets : [ "uitheme", "filter" ],
        widgetOptions : {
            filter_hideFilters: false,
        }
    });
});
